# Minimal local dependencies for the current repository state.
#
# - The installable CLI and smoke mode use only the standard library.
# - The real root-based pipeline imports the optional debug Excel exporter at
#   module import time, so openpyxl remains a lightweight runtime dependency.
# - The default test suite imports pytest directly.
#
# Heavy optional ML dependencies for the full root pipeline are intentionally
# not installed here. They should be added only in environments that need
# those specific branches.
openpyxl>=3.1,<4
pytest>=8,<9
