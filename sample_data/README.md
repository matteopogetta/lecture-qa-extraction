# Sample data

This directory is reserved for tiny sample files only.

- Do not commit heavy lecture media files.
- Keep tracked sample assets small enough for Git review and cloning.
- For real experiments, copy local media here temporarily or point the CLI to a
  file outside the repository.

The tracked `example.mp4` file is only a placeholder used to validate the
bootstrap CLI and Docker workflow.
